
/**
 * getName() prints name (parameter)
 *
 * @param {string} name -
 *
 * @return {void} void
 */
function getName(name) {
  let tempName = name.toUpperCase();
  console.log(tempName)
}


/**
 * sumbNumbers() returns the sum of two numbers
 *
 * @param {int} num1 - the first number
 * @param {int} num2 - the second number
 *
 * @return {int} the sumb of both numbers
 */
function sumbNumbers(num1, num2) {
  return num1 + num2
}
